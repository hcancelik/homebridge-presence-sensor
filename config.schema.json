{
  "pluginAlias": "PresenceSensorPlugin",
  "pluginType": "platform",
  "singular": true,
  "strictValidation": false,
  "schema": {
    "type": "object",
    "properties": {
      "maxStationaryDistance": {
        "title": "Max Stationary Distance",
        "type": "number",
        "default": 150,
        "description": "The maximum distance a device should monitor, in centimeters."
      },
      "minStationarySignal": {
        "title": "Min Stationary Signal",
        "type": "number",
        "default": 15,
        "description": "The minimum stationary signal should considered as presence (between 0 and 100)."
      },
      "maxMovingDistance": {
        "title": "Max Moving Distance",
        "type": "number",
        "default": 150,
        "description": "The maximum distance a device should monitor, in centimeters."
      },
      "minMovingSignal": {
        "title": "Min Moving Signal",
        "type": "number",
        "default": 15,
        "description": "The minimum moving signal should considered as presence (between 0 and 100)."
      },
      "turnOffTimeout": {
        "title": "Turn Off Timeout (seconds)",
        "type": "number",
        "default": 3,
        "description": "The threshold to consider real no motion. Increase this value if you have false positives."
      },
      "mqttHost": {
        "title": "MQTT Host",
        "type": "string",
        "default": "mqtt://localhost",
        "description": "The MQTT host to connect to."
      },
      "mqttUsername": {
        "title": "MQTT Username",
        "type": "string",
        "default": "",
        "description": "The MQTT username to use."
      },
      "mqttPassword": {
        "title": "MQTT Password",
        "type": "string",
        "default": "",
        "description": "The MQTT password to use."
      },
      "mqttTopic": {
        "title": "MQTT Topic",
        "type": "string",
        "default": "presence",
        "description": "The MQTT topic to publish to."
      }
    }
  }
}
